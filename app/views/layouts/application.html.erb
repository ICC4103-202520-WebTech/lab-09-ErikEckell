<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Lab 07 – Recipe Book (Erik Eckell)" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Favicon -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap CSS via CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-w76A6E2FwW+6Bz5i0u2z9k9sQx1U4U+e7b1q1CjSMeh3GcD/0ThhKp5V2QK1Qn8+"
      crossorigin="anonymous"
    >

    <!-- Rails styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Rails importmap JS -->
    <%= javascript_importmap_tags %>

    <!-- Bootstrap JS Bundle via CDN (Popper incluido) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-qkXIo0Y+6I0mHl6mBctn4tVvK8bXgQf2Z06u+m2qg9cB28Zp3cHf4oK2fx7eZixf"
      crossorigin="anonymous"
      defer
    ></script>
  </head>

  <body class="bg-light min-vh-100 d-flex flex-column">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm mb-4">
      <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="<%= root_path %>">🍳 Recipe Book</a>

        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
  <li class="nav-item">
    <%= link_to "All Recipes", recipes_path, class: "nav-link" %>
  </li>

  <% if user_signed_in? && can?(:create, Recipe) %>
    <li class="nav-item">
      <%= link_to "New Recipe", new_recipe_path, class: "nav-link" %>
    </li>
  <% end %>

  <% if user_signed_in? && current_user.admin? %>
    <li class="nav-item">
      <%= link_to "Manage Users", users_path, class: "nav-link" %>
    </li>
  <% end %>
</ul>


          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <span class="navbar-text text-light me-3">
                  👤 <%= current_user.email %>
                  <% if current_user.admin? %>
                    <span class="badge bg-primary">Admin</span>
                  <% end %>
                </span>
              </li>
              <li class="nav-item">
                <%= link_to "Log out",
                    destroy_user_session_path,
                    data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to log out?" },
                    class: "nav-link text-danger fw-semibold" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Main content -->
    <main class="container flex-grow-1 mb-5">
      <% flash.each do |key, message| %>
        <% bootstrap_class =
          case key
          when "notice" then "alert-success"
          when "alert", "error" then "alert-danger"
          else "alert-info"
          end %>

        <div class="alert <%= bootstrap_class %> alert-dismissible fade show text-center shadow-sm" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-3 mt-auto text-center small">
      © <%= Time.current.year %> Erik Eckell — Recipe Book
    </footer>
  </body>
</html>
